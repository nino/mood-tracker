import React from 'react';
import Button from './Button';
import styled from 'styled-components';

/**
 * Renders a group of buttons.
 * Most importantly, `Button`s are rendered without any margins,
 * rounded corners, or shadows. This is handled by the enclosing 
 * div to avoid ugly overlapping effects.
 * The `Button`s are spaced evenly on the horizontal axis.
 */
const ButtonGroup = ({ buttons }) => (
  <Wrapper>
  </Wrapper>
);

ButtonGroup.propTypes = {
  buttons: 
};

export default ButtonGroup;
