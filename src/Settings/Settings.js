import React, {Component} from 'react'

class Settings extends Component {
    propTypes: {
        metrics: React.PropTypes.array.isRequired,
        onAction: Recat.PropTypes.func.isRequired
    }

    render() {
        return <div>Settings ...</div>
    }
}

export default Settings
